import React from "react";
import { useNavigate } from "react-router-dom";

const IterativeDesign = (props) => {
    const navigate = useNavigate();


    return (
        <div>
            <h1>IterativeDesign</h1>
        </div>
    )
}

export default IterativeDesign;